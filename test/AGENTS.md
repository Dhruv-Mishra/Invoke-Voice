# Tests

- Use `node:test` and public behavior; avoid implementation-coupled mocks.
- Cover state transitions, contract boundaries, security checks, and regressions.
- Keep fixtures local and deterministic; never require provider credentials or model downloads.